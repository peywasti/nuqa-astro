---

---

<html lang="fa" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content="دامنه nuqa برای فروش" />
    <title>nuqa — دامنه برای فروش</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;700&family=Playfair+Display:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <style is:global>
      :root {
        --bg: #0c0c0a;
        --fg: #e8e4dc;
        --fg-dim: #8a8580;
        --accent: #c4a882;
        --border: rgba(232, 228, 220, 0.1);
        --font-fa: "Vazirmatn", system-ui, sans-serif;
        --font-en: "Playfair Display", Georgia, serif;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: var(--font-fa);
        background-color: var(--bg);
        color: var(--fg);
        direction: rtl;
        min-height: 100dvh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
        overflow: hidden;
      }

      .grain {
        position: fixed;
        inset: 0;
        pointer-events: none;
        z-index: 9999;
        opacity: 0.035;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      }

      .card {
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.25rem;
        max-width: 520px;
      }

      .title {
        font-family: var(--font-en);
        font-size: clamp(4.5rem, 14vw, 10rem);
        font-weight: 400;
        letter-spacing: -0.04em;
        line-height: 1;
        color: var(--fg);
        margin-bottom: 1.5rem;
      }

      .badge {
        font-family: var(--font-fa);
        font-size: 0.9375rem;
        font-weight: 500;
        color: var(--accent);
        border: 1px solid var(--border);
        padding: 0.4rem 1.25rem;
        border-radius: 100px;
      }

      .desc {
        font-size: 0.9375rem;
        color: var(--fg-dim);
        line-height: 1.9;
        max-width: 400px;
      }

      .divider {
        width: 32px;
        height: 1px;
        background: var(--border);
        margin: 0.25rem 0;
      }

      .email {
        font-family: var(--font-en);
        font-size: clamp(1.25rem, 3vw, 1.75rem);
        color: var(--fg);
        text-decoration: none;
        letter-spacing: -0.01em;
        border-bottom: 1px solid transparent;
        padding-bottom: 2px;
        transition:
          color 0.3s,
          border-color 0.3s;
      }

      .email:hover {
        color: var(--accent);
        border-bottom-color: var(--accent);
      }

      .copy {
        font-size: 0.75rem;
        color: var(--fg-dim);
        margin-top: 0.5rem;
      }

      @media (max-width: 480px) {
        .card {
          gap: 1rem;
        }

        .title {
          font-size: clamp(3.5rem, 18vw, 5rem);
        }
      }
    </style>
  </head>
  <body>
    <div class="grain" aria-hidden="true"></div>

    <main class="card">
      <h1 class="title">nuqa</h1>
      <span class="badge">دامنه nuqa.ir برای فروش است</span>
      <p class="desc">
        دامنه‌ای کوتاه، خاص و به‌یادماندنی. در هر صنعتی قابل استفاده؛ از
        تکنولوژی تا هنر.
      </p>
      <div class="divider"></div>
      <a href="mailto:abbaszadeh.h@gmail.com" class="email"
        >abbaszadeh.h@gmail.com</a
      >
      <span class="copy">© ۲۰۲۶ nuqa</span>
    </main>
  </body>
</html>
